import React from 'react';
import './Card.css';

function Card() {
  // Por enquanto, o card é apenas visual.
  // A lógica de virar a carta virá depois.
  return (
    <div className="card">
      {/* O conteúdo (ícone/imagem) será adicionado aqui futuramente */}
    </div>
  );
}

export default Card;